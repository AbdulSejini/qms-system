# الأمان (Security) — نظام مراجعة الجودة QMS

هذه الوثيقة تصف نموذج الأمان الفعلي لقاعدة البيانات (Firestore) بعد الانتقال الكامل إلى Firebase
Authentication: ما الذي يمنح الصلاحية، وما الذي تفرضه القواعد في `firestore.rules` وما لا تفرضه،
وكيف يُنشأ أول مدير نظام، وكيف يحصل الموظفون القدامى على حسابات دخول، وماذا يفعل المشغّل خطوة بخطوة.

الوثيقة موجّهة لمن يملك صلاحية النشر على مشروع Firebase (`qms-saudicable`). اقرأها كاملة قبل تعديل
`firestore.rules` أو نشره.

> **ملاحظة على المراجع**: تُذكر المراجع هنا باسم الملف واسم الدالة أو الثابت، لا برقم السطر. أرقام
> الأسطر تتقادم مع أول تعديل وتحوّل الوثيقة إلى مصدر غير موثوق، وقد حدث ذلك في نسخ سابقة من هذا
> الملف. كل ادعاء في هذه النسخة مُتحقَّق منه مقابل شجرة العمل وقت الكتابة.

> ⚠️ **الخلاصة أولاً**
> 1. النظام متوقف منذ 3 مارس 2026 لأن قاعدة وضع الاختبار
>    (`allow read, write: if request.time < timestamp.date(2026, 3, 3)`) انتهت صلاحيتها، فلا قراءة
>    ولا كتابة تنجح اليوم.
> 2. القواعد الجديدة تعيد التشغيل وتفرض الصلاحيات فعلياً على أساس مستند ربط واحد
>    (`authUsers/{authUid}`)، وليس على أساس امتلاك حساب في Firebase Auth.
> 3. **الوصول المجهول (unauthenticated) محصور في قراءة واحدة**: `get` على المستند الواحد
>    `users/system-admin-root`، وهي مسموحة فقط ما دام هذا المستند بلا `authUid` — أي فقط قبل
>    اكتمال التهيئة الأولى. مسار الدخول القديم (الترحيل الكسول) حُذف من الشيفرة. التفصيل في
>    القسمين 4 و 5.0.
> 4. **مجموعة `passwords` أصبحت خاملة تماماً**: لا يقرؤها أحد، ولا يكتبها أحد، والقواعد ترفض كل
>    قراءة وكل كتابة عليها لكل شخص. القسم 4.4 يحدد بدقة متى يحذفها المشغّل.
> 5. **الموظف القديم يُمنح حساب دخول من مدير النظام** من صفحة المستخدمين، لا بكلمة مروره القديمة.
>    الآلية في القسم 5.2.
> 6. **أول مستند ربط يُنشأ من داخل التطبيق نفسه، مرة واحدة، من صفحة `/login`** — بلا أي عمل في
>    Firebase Console. القواعد تفتح لذلك نافذة واحدة **تُغلق نفسها** بمجرد استعمالها. القسم 5.0
>    يشرح ما تفتحه بالضبط، وكيف تُغلق، **والخطر القائم بين لحظة نشر القواعد ولحظة إتمام التهيئة**.
>    الخطوة اليدوية من Console (القسم 5.1) بقيت كمسار بديل عند تعذّر المسار داخل التطبيق.
> 7. **التزام المشغّل**: انشر القواعد وأتمم التهيئة في الجلسة نفسها. النافذة مفتوحة بينهما، ومن
>    يصل إلى `/login` أولاً يملك حساب مدير النظام.
> 8. **مدير إدارة الجودة صار يُنشئ المستخدمين ويمنحهم حسابات الدخول** — لا مدير النظام وحده. ومعه
>    سقف صريح مكتوب في القواعد: لا يكتب دور `system_admin` لأحد ولا لنفسه، ولا يلمس حساب مدير
>    النظام ولا أي حساب يحمل دوره، ولا يكتب مستند الربط الخاص به هو. القسم 2.2.
> 9. **`activityLog` سجل نشاط دائم**: يقرؤه **مدير النظام وحده**، ويكتبه كل موظف نشط عن نفسه،
>    و**لا يُعدَّل ولا يُحذف من التطبيق إطلاقاً — ولا حتى بيد مدير النظام**. القسم 2.3.
>    وميزة «من متصل الآن» أُلغيت، ومجموعة `activeSessions` أُغلقت للجميع. القسم 3.4.

> 🛑 **قبل أي شيء: خذ نسخة احتياطية.** النسخة الوحيدة الموجودة في المستودع
> (`backups/firestore-2026-08-11T11-20-42-997Z.json`) **فاشلة بالكامل**: مجموعاتها السبع كلها تحمل
> `"error": "Missing or insufficient permissions."` لأن القواعد المنتهية كانت ترفض كل قراءة. أي
> نسخة احتياطية صالحة لا يمكن أخذها إلا **بعد** نشر القواعد الجديدة وإنشاء ربط مدير النظام
> (`node scripts/backup-firestore.mjs`، انظر `scripts/README.md` والقسم 4.4).

---

## 1. نموذج الأمان

### 1.1 امتلاك حساب في Firebase Auth لا يمنح شيئاً

طريقة Email/Password مفعّلة في المشروع، والتسجيل الذاتي (self-signup) يبقى مفعّلاً لأن صفحة
المستخدمين تحتاجه: إنشاء حساب الدخول لموظف يتم من المتصفح عبر `createUserWithEmailAndPassword` على
**نسخة Firebase ثانوية** حتى لا تُستبدل جلسة المسؤول (`getSecondaryAuth` في `src/lib/auth.ts`،
و`getUserCreationAuth` في `src/app/users/page.tsx`). لا يوجد `firebase-admin` ولا حساب خدمة،
والمشروع على خطة Spark (لا Cloud Functions)، فلا مفر من إنشاء الحساب من العميل.

ولأن أي شخص على الإنترنت يستطيع إذن أن يُنشئ لنفسه حساباً بأي بريد يكتبه — Firebase لا يتحقق من
ملكية البريد عند الإنشاء — فإن القواعد **لا تعتمد على حساب المصادقة إطلاقاً**، ولا على
`request.auth.token.email`. مصدر الصلاحية الوحيد هو مستند واحد:

```
authUsers/{authUid} = { userId: string, role: string, isActive: bool }
```

وهذا المستند **لا يستطيع صاحبه إنشاءه ولا تعديله**، باستثناء واحد: النافذة الأولية المذكورة في
القسم 5.0، وهي تُنشئ أول مستند ربط لحساب مدير النظام وحده، مرة واحدة، ثم تُغلق نفسها نهائياً. فيما
عدا ذلك يكتبه مدير نظام أو مدير إدارة جودة مُصادَق عليه — **ولا يكتب أيٌّ منهما مستند ربطه هو، ولا
مستند ربط مدير النظام** (القسم 2.2). النتيجة: الغريب الذي يسجّل حساباً — بعد إغلاق النافذة — يصل
إلى نظام يرفض له كل قراءة وكل كتابة، بما في ذلك قراءة دليل الموظفين.

### 1.2 الثغرة التي أُزيلت: المطالبة الذاتية (self-claim)

نسخة سابقة من `firestore.rules` كانت تسمح للمستخدم بإنشاء مستند الربط الخاص به إذا طابق
`request.auth.token.email` بريد مستند مستخدم موجود، ويأخذ منه الدور. مجتمعةً مع أمرين آخرين كان
هذا يعني **ترقية صلاحيات كاملة لأي شخص على الإنترنت**:

1. البريد على رمز Auth هو ما كتبه المستخدم عند التسجيل، ولا يُتحقق منه.
2. القواعد نفسها كانت تسمح بقراءة `users` بدون مصادقة، أي أن بريد كل موظف كان معلوماً للجميع.
   وبريد مدير النظام مكتوب أصلاً في الشيفرة المصدرية: الثابت `SYSTEM_ADMIN_EMAIL` في
   `src/lib/firestore.ts`.

أي أن الخطوات كانت: سجّل حساباً ببريد مدير النظام ← اكتب مستند الربط لنفسك بدور `system_admin` ←
لك النظام كله. أُزيل هذا المسار بالكامل.

**ما بقي منه — بحدود محسوبة — هو النافذة الأولية (القسم 5.0)**، والفرق بينهما ليس فرق درجة:

| المطالبة الذاتية القديمة | النافذة الأولية |
| --- | --- |
| تعتمد على `request.auth.token.email` وهو ما كتبه المستخدم | تتجاهل بريد الرمز تماماً |
| تمنح أي دور لأي مستخدم مطابق بالبريد | تكتب حمولة واحدة ثابتة: `system-admin-root` + `system_admin` + `isActive: true` |
| مفتوحة إلى الأبد | تُغلق نهائياً بمجرد أن يحمل `users/system-admin-root` حقل `authUid` |
| قابلة للتكرار | تعمل مرة واحدة في عمر قاعدة البيانات |

وفيما عدا هذه النافذة، القراءة فقط مسموحة لصاحب مستند الربط، لأن تسجيل الدخول يحتاجها.

### 1.3 لماذا لم تُعَد ترقيم معرّفات المستخدمين

معرّفات مستندات `users` القديمة (`system-admin-root`, `user-1739...`) مشار إليها في كل مكان:
`leadAuditorId`, `teamMemberIds`, `createdBy`, `approverId`, `approvedBy`, `responsibleId`,
`recipientId`, `senderId`, و `actorUserId` في سجل النشاط (ومعرّف مستند الجلسة في `activeSessions`
المغلقة الآن — القسم 3.4). إعادة ترقيمها إلى Auth UID
تعني إعادة كتابة بيانات حقيقية بكاملها. مستند الربط هو البديل: طبقة ترجمة رخيصة بين العالمين،
تُقرأ في القواعد بـ `get()` واحد على مسار معلوم.

---

## 2. كيف تقرأ القواعد هوية المتصل — الدالة `isActive()`

كل قاعدة في الملف مبنية على بوابة واحدة:

```
function isActive() {
  return isSignedIn()                                          // 1
         && exists(mappingPath())                              // 2
         && mapping().get('isActive', false) == true           // 3
         && mapping().get('userId', '') is string
         && mapping().get('userId', '') != ''
         && exists(userPath(mapping().get('userId', '')))      // 4
         && callerUserDoc().get('isActive', true) != false;    // 5
}
```

وهي في الوقت نفسه **آلية سحب الصلاحية**:

| الحالة | النتيجة |
| --- | --- |
| لا يوجد حساب Auth | مرفوض (لا وصول مجهول في أي مجموعة، عدا قراءة واحدة موصوفة في 5.0) |
| حساب Auth بلا مستند ربط | مرفوض — هذه حالة الغريب الذي سجّل نفسه، وحالة الموظف قبل أن يُنشئ له المسؤول حساباً |
| `authUsers/{uid}.isActive != true` | مرفوض — علم التعطيل على مستند الربط نفسه |
| `users/{mapping.userId}` محذوف | مرفوض فوراً، حتى لو بقي مستند الربط وحساب Auth |
| `users/{mapping.userId}.isActive == false` | مرفوض — الشرط رقم 5، ويُقرأ من **مستند المستخدم مباشرة** |

**لماذا أُضيف الشرط الخامس (وهو ثغرة أُغلقت، لا احتياط مكرر):** `saveUser` في
`src/lib/firestore.ts` لا يُزامن `isActive` على مستند الربط إلا داخل `if (authUid)` — أي فقط حين
يكون مستند المستخدم يحمل `authUid` بالفعل. فالموظف الذي عُطِّل من صفحة المستخدمين قبل أن يُكتب
عليه هذا المؤشّر كان يبقى مستند ربطه يقول `isActive: true`، والقواعد كانت تقرأ الربط وحده،
**فيحتفظ بكامل صلاحياته بعد تعطيله**. مستند المستخدم هو السجل الذي تكتبه صفحة المستخدمين دائماً،
فصار هو المقروء. والقيمة الغائبة أو `null` تُعدّ «نشطاً»، مطابقةً لـ `isUserActive` في
`src/lib/auth.ts` (`user.isActive !== false`).

`appRole()` و `appUserId()` تُرجعان `''` لمن لا يجتاز البوابة، ومعرّف المستند لا يكون `''` أبداً،
فلا يمكن أن يطابق أحدٌ بالخطأ.

**التكلفة**: كل عملية تلمس مستندين إضافيين في القواعد (مستند الربط، ومستند المستخدم الذي صار
يُقرأ لا يُفحص وجوده فقط). Firestore يخزّن نتيجة الوصول إلى المسار نفسه داخل التقييم الواحد، والحد
الأقصى 10 استدعاءات وصول لطلب المستند الواحد، فالهامش واسع حتى في قواعد الخطة السنوية التي تستدعي
`isActive()` عدة مرات.

### 2.1 الأدوار

الأدوار الست معرّفة في `src/types/index.ts`، والصلاحيات الافتراضية لكل دور في
`DEFAULT_PERMISSIONS` (`src/types/index.ts:30-50` للدورين اللذين تستعملهما القواعد). القواعد تستعمل
تجميعتين فقط:

- `isAdmin()` = `system_admin`: **قراءة** سجل النشاط `activityLog`، وسرد مجموعة `authUsers`،
  وكل ما يمسّ حساب مدير النظام نفسه أو أي حساب يحمل دور `system_admin` (القسم 2.2).
- `isQualityStaff()` = `system_admin` + `quality_manager`: الإدارات، الأقسام، حذف المراجعات،
  وإنشاء/تحرير الخطة السنوية — **وكذلك المستخدمون ومستندات الربط بعد التوسعة، بسقف** القسم 2.2.
  وهما الدوران اللذان يمنحهما `DEFAULT_PERMISSIONS` `canManageDepartments` و `canDeleteAudits`
  و `canApproveAudits`.

**الإدارات والأقسام لمدير الجودة — مؤكَّد على الجانبين**: القواعد المنشورة اليوم تُسند
`departments` و `sections` إلى `isQualityStaff()` (لم يتغيّر شيء هنا في هذه المراجعة)،
و `DEFAULT_PERMISSIONS` يمنح `canManageDepartments`
لـ `quality_manager`، فالطرفان متفقان ولا شيء هنا يحتاج تعديلاً. (بل إن `src/app/departments/page.tsx`
**لا يفحص الصلاحية أصلاً**: يقرأ `canManageDepartments` من `useAuth` ولا يستعمله، فأزرار الإضافة
والتعديل والحذف ظاهرة لكل موظف نشط والقواعد وحدها ترفض كتابته. الصفحة أوسع من القاعدة، لا أضيق
منها، فلا شيء يُمنع عن مدير الجودة — لكنه عيب واجهة يستحق الإصلاح: انظر القسم 8، البند 3.)

**اعتماد الخطة السنوية ليس دوراً**: القواعد لا تسأل عن دور المعتمِد إطلاقاً، بل عن هويته المكتوبة
في `approverId` على الخطة نفسها. انظر القسم 3.1.

### 2.2 مدير إدارة الجودة يُنشئ المستخدمين — وما الذي يمنعه من الترقّي

كانت `users` و `authUsers` حكراً على `isAdmin()`. صارت الآن `isQualityStaff()`، أي أن مدير إدارة
الجودة يستطيع إنشاء الموظف وتعديله وحذفه، **وإصدار مستند الربط** الذي يمنحه الدخول. التوسعة
مقصودة: المالك لا يريد أن يمرّ كل موظف جديد به، ومستخدم بلا مستند ربط مستخدمٌ يُصادَق عليه ويُرفض
في كل قاعدة — فإنشاء المستخدم بلا صلاحية إصدار الربط لا معنى له.

لكن **مستند الربط هو الصلاحية نفسها**، فالتوسعة بلا سقف تُقرأ حرفياً: «مدير الجودة يستطيع ترقية
نفسه إلى `system_admin`». السقف مكتوب في `firestore.rules` صراحةً على كل قاعدة من الأربع، ولا يمسّ
مدير النظام لأنه يمرّ من فرع `isAdmin()` قبله:

| الحاجز | أين هو | ما الذي يمنعه بالضبط |
| --- | --- | --- |
| **لا يُكتب دور `system_admin`** | `writesSystemAdminRole()` على `users`، وشرط `role != 'system_admin'` على `authUsers` | الترقية بخطوتيها. `claimMatchesUserDocument()` يشترط تطابق دور مستند الربط مع دور مستند المستخدم، فالمنع على الجانبين يجعل نصف الزوج غير قابل للبناء وحده — إنشاءً كان أو تعديلاً (والترقية تعديل) |
| **لا يُلمس حساب مدير النظام** | `isSystemAdminDocument()` و `touchesSystemAdminUser()` | تعديل `users/system-admin-root` أو حذفه أو تعطيله، وكتابة مستند ربطه أو تعديله أو حذفه. تعطيل مستند ربط المالك أو خفض دوره **إخراجٌ للمالك من نظامه** بيد الدور الذي فوّضه، لا مجرد تعديل بيانات |
| **لا يُلمس أي حساب يحمل دور `system_admin`** | `targetHoldsSystemAdminRole()` و `existingGrantIsSystemAdmin()` | نفس الحماية لو وُجد مدير نظام آخر غير `system-admin-root`. تُقرأ من المستند **القائم**، لا من المطلوب كتابته |
| **لا يكتب مستند ربطه هو** | `isOwnMapping()` | لا بالـ UID، ولا بالمعرّف الذي يشير إليه، ولا بمستند ربط ثانٍ يصنعه لحساب Auth يملكه ويجعله يشير إلى نفسه. من لا يستطيع إعادة كتابة ترخيصه لا يستطيع توسيعه لاحقاً بهدوء |
| **لا يغيّر دوره هو** | `ownRoleUnchanged()` على `users` | إعادة كتابة القيمة نفسها ليست تغييراً، فتعديل الملف الشخصي العادي يمرّ. وحده تحويل الدور يُرفض |
| **لا يحذف مستند مستخدمه هو** | `!isSelf(userId)` على حذف `users` | إغلاق ذاتي غير قابل للإصلاح من التطبيق: `isActive()` تشترط وجود مستند المستخدم، فحذفه يقطع صاحبه فوراً ويترك مستند ربط يتيماً |

ملاحظتان تقنيتان تُغني عن مفاجأتين لاحقاً:

- **حذف `passwords/{id}` تبع `users`**: `deleteUser` في `src/lib/firestore.ts` يحذف مستند المستخدم
  ثم مستند كلمة المرور القديمة في نداء واحد. لو بقي حذف `passwords` على `isAdmin()` وحده لكان كل
  حذف يجريه مدير الجودة يفشل **بعد** أن يكون مستند المستخدم قد حُذف فعلاً. لذلك صار حذف `passwords`
  هو أيضاً `isQualityStaff()`. القراءة والكتابة عليها ما زالتا مرفوضتين للجميع (القسم 4).
- **حذف مستند الربط**: مستدعيه الحيّ الوحيد هو `rollbackCreatedAccount` في `src/lib/auth.ts` حين
  يفشل تمكين موظف في منتصفه. فتُرك لمن أجرى التمكين، بنفس الحواجز الثلاثة أعلاه، وحذف مستند غير
  موجود مسموح عمداً — رفضه يحوّل تراجعاً نظيفاً إلى فشل مُسجَّل.

**والواجهة؟** `src/app/users/page.tsx` كان يقبل الدورين أصلاً في شرط `hasAccess`، وقائمة الأدوار
فيه تستبعد `system_admin` من الإنشاء **للجميع**، فما تعرضه الصفحة صار مطابقاً لما تسمح به القواعد.
البقية عيب واجهة قديم: `DEFAULT_PERMISSIONS` يمنح `canManageUsers` لمدير النظام وحده، والصفحة
تقرأ `canManageUsers` من `useAuth` ولا تستعمله في أي شرط. لا أثر أمني لذلك بعد هذه المراجعة، لكنه
تناقض يجب أن يُحسم في الشيفرة — القسم 8، البند 3.

### 2.3 سجل النشاط `activityLog` — للمالك وحده، ولا يُعدَّل ولا يُحذف

`activityLog` (`src/lib/activity-log.ts`، والشكل `ActivityEntry` في `src/types/index.ts`) سجل
دائم لمن دخل، ومن أنشأ أو عدّل أو حذف شيئاً. هو بديل ميزة «من متصل الآن»: قائمة النوافذ المفتوحة
لم تكن تجيب عن سؤال، ونظام مراجعة داخلية يُتوقَّع منه أن يجيب عن «من فعل ماذا ومتى» بعد شهور.

| العملية | من | لماذا |
| --- | --- | --- |
| `get` / `list` | **مدير النظام وحده** | هذا سجلّ المالك الخاص. ليس لمدير الجودة، ولا لصاحب القيد نفسه. سجلٌّ يقرؤه المسجَّل عليهم شيء آخر، وأثره على سلوكهم آخر. البوابة هنا **أضيق** من كل بوابات الملف |
| `create` | أي موظف نشط، بشرط أن يكون `actorUserId` في المستند الوارد **هو هويته هو** | ما يملأ السجل هو أفعال الناس أنفسهم، فالكاتب هو الفاعل لا المسؤول. والشرط يمنع كتابة قيد باسم شخص آخر: سجلٌّ يمكن أن يُكتب بأسماء الآخرين أسوأ من لا سجل — إنه دليل يشير إلى الموظف الخطأ |
| `update` / `delete` | **لا أحد. أبداً. ولا مدير النظام** | سجلٌّ يستطيع صاحبه إعادة كتابته ليس سجلّ مراجعة. قيمة هذه المجموعة كلها قائمة على أن أحداً لا يستطيع تنقيحها بعد وقوع الفعل، و«أحد» يجب أن تشمل صاحب أقوى دافع وأوسع وصول. `src/lib/activity-log.ts` لا يُصدّر دالة تعديل ولا دالة حذف للسبب نفسه؛ والقاعدة هي ما يحوّل ذلك من عُرف إلى ضمان، لأنها تصمد مهما تغيّرت الشيفرة لاحقاً |

**التصحيح أو التقليم — إن لزم يوماً — من Firebase Console وحدها**، لأن القواعد لا تسري عليها: فعلٌ
مقصود، ظاهر، وخارج التطبيق. هذا هو الفرق بين «تعديل غير ممكن» و«تعديل يترك أثراً».

وحدّان يجب أن يعرفهما من يقرأ السجل على أنه كامل:

1. **محاولات الدخول الفاشلة لا تُسجَّل.** صاحبها غير مصادق عليه أو بلا مستند ربط، فـ `isActive()`
   ترفضه ولا توجد له هوية صادقة تُكتب في `actorUserId`. البديل كان السماح لمجهول بالكتابة في
   السجل، وهي مقايضة أسوأ. محاولات الدخول الفاشلة موجودة في Firebase Console → Authentication.
2. **قيد `logout` يجب أن يُكتب قبل `signOut()`**، فبعده لا يوجد `request.auth` وتُرفض الكتابة.
   `recordActivity` لا تنتظر نتيجة الكتابة وتبتلع الفشل، فلا يظهر شيء من ذلك أمام المستخدم في
   الحالتين.

---

## 3. ما تفرضه القواعد لكل مجموعة

| المجموعة | قراءة (get / list) | إنشاء / تعديل | حذف |
| --- | --- | --- | --- |
| `users` | `get`: أي موظف نشط — **وزائر غير مصادق عليه لمستند `system-admin-root` وحده وما دام بلا `authUid`** (5.0) — `list`: أي موظف نشط | `system_admin` + `quality_manager` بسقف القسم 2.2، ويُضاف في التحديث كتابة `authUid` على `system-admin-root` ضمن النافذة الأولية (5.0) | `system_admin` + `quality_manager` بسقف 2.2 (ولا يحذف أحد مستنده هو) |
| `authUsers` | `get`: صاحب المستند أو موظفو الجودة (مدير النظام ومدير الجودة) — `list`: مدير النظام | `system_admin` + `quality_manager` بسقف 2.2، بشرط الشكل الصحيح ومطابقة الدور لمستند المستخدم. **إنشاء** فقط: تُضاف مطالبة النافذة الأولية (5.0) | `system_admin` + `quality_manager` بسقف 2.2 |
| `passwords` | **مرفوضة للجميع** | **مرفوضة للجميع** | `system_admin` + `quality_manager` — تبعاً لحذف المستخدم وحده (2.2 و 4.4) |
| `departments` | أي موظف نشط | `system_admin` + `quality_manager` | نفسهما |
| `sections` | أي موظف نشط | `system_admin` + `quality_manager` | نفسهما |
| `audits` | أي موظف نشط | إنشاء: أي موظف نشط بشرط `createdBy` = هويته — تعديل: أطراف المراجعة وحدهم (`belongsToAudit`): إدارة الجودة، ورئيس الفريق وأعضاؤه، والجهة المُراجَع عليها المسمّاة في `auditeeId` | `system_admin` + `quality_manager` |
| `annualPlans` | أي موظف نشط | حسب دورة الحياة — سبعة مسارات، انظر 3.1 | صاحبها أو مدير النظام، وللمسودة والمرفوضة فقط، وتذهب إشعاراتها معها (3.3) |
| `activeSessions` | **مرفوضة للجميع** (3.4) | **مرفوضة للجميع** | **مرفوضة للجميع** |
| `activityLog` | **مدير النظام وحده** (2.3) | إنشاء: أي موظف نشط بشرط `actorUserId` = هويته — **ولا تعديل لأحد إطلاقاً** | **مرفوض للجميع، بمن فيهم مدير النظام** |
| `notifications` | المستقبِل أو المُرسِل (أو مدير النظام) | إنشاء: أي موظف نشط، و `senderId` — إن وُجد — يجب أن يكون هويته — وتعديل: `read` وحده | المستقبِل أو المُرسِل (أو مدير النظام) — انظر 3.3 |

وكل مجموعة أو مستند غير مذكور أعلاه مرفوض بقاعدة `match /{document=**} { allow read, write: if false; }`.

### 3.1 الخطة السنوية: دورة الحياة مفروضة في القواعد، لا في الواجهة

هذا هو التغيير الأكبر في هذه المراجعة. القاعدة السابقة كانت تقول عملياً "أي مدير جودة يعدّل أي
خطة"، وهو ما ألغى خطوة الاعتماد التي طلبها المالك: كاتب الخطة كان يستطيع اعتماد خطته بنفسه، وكان
المعتمِد يستطيع تغيير قراره لاحقاً، وكانت الخطة المعتمدة تبقى قابلة للكتابة في قاعدة البيانات وإن
بدت مقفلة في الواجهة.

المسارات المسموحة الآن — ولا شيء غيرها:

| # | من | متى (الحالة الحالية) | ماذا يكتب | إلى |
| --- | --- | --- | --- | --- |
| 1 | كاتب الخطة أو مدير النظام (وبشرط `isQualityStaff`) | `draft` أو `rejected` | أي حقل، مع تجميد `createdBy` و `year`، وأختام القرار لا تُكتب — تُمسح أو تُترك كما هي | نفس الحالة |
| 2 | كاتب الخطة أو مدير النظام (وبشرط `isQualityStaff`) | `draft` أو `rejected` | `status`, `approverId`, `submittedAt`, `updatedAt`، ويجوز معها **مسح** أختام القرار | `pending_approval` |
| 3 | **المعتمِد المسمّى في `approverId` وحده** | `pending_approval` فقط | حقول القرار وحدها، وبختم يحمل هويته هو | `approved` أو `rejected` |
| 4 | **كاتب الخطة أو مدير النظام** (سحب الطلب) | `pending_approval` فقط | `status`، ويجوز معه إفراغ `approverId` و `submittedAt` ومسح الأختام — ولا يجوز تحويل `approverId` إلى شخص آخر | `draft` |
| 5 | أي موظف نشط (من ينشئ المراجعة) | `approved` فقط | `items` و `updatedAt` فقط، وبشرط أن يكون الفارق **بنداً واحداً يختلف في `auditId` وحده** | `approved` (بلا تغيير حالة) |

وما يترتب على ذلك مباشرة:

- **لا أحد يعتمد خطته**. عند الإرسال (المسار 2) ترفض القاعدة أن يكون `approverId` مساوياً لـ
  `createdBy` أو لهوية المُرسِل. وعند القرار (المسار 3) تُعاد نفس المقارنة، لأن خططاً كُتبت قبل نشر
  هذه القواعد قد تحمل بالفعل معتمِداً هو كاتبها.
- **القرار لا يُعاد**. المسار 3 مشروط بأن تكون الحالة الحالية `pending_approval`، فبمجرد أن تصبح
  الخطة `approved` أو `rejected` لا يطابقها أي مسار.
- **الخطة المعتمدة نهائية في قاعدة البيانات — إلا حقلاً واحداً**: لا تعديل ولا حذف، ولا حتى لمدير
  النظام، فيما عدا المسار 5 (`items[].auditId`). تصحيح خطة اعتُمدت بالخطأ عملٌ متعمّد من Firebase
  Console (قواعد Firestore لا تُطبَّق على Console ولا على Admin SDK)، ويُفترض أن يُوثَّق كما يُوثَّق
  أي تعديل يدوي على بيانات حقيقية.
- **المحتوى مجمّد من لحظة الإرسال**: لا يستطيع الكاتب تعديل البنود وهي `pending_approval`، فما
  يقرّره المعتمِد هو ما عُرض عليه.
- **أختام القرار** (`approvedBy`, `approvedAt`, `rejectedBy`, `rejectedAt`, `rejectionReason`,
  `approverComment`) لا يكتب الكاتب فيها قيمة أبداً، وختم القرار يجب أن يحمل هوية من يكتبه.
- **إنشاء الخطة لا يحمل قراراً**: قاعدة الإنشاء ترفض أي خطة تولد ومعها ختم قرار. بدون هذا الشرط
  كان يمكن إنشاء خطة تقول `approvedBy: <أي أحد>` منذ لحظتها الأولى، أي اعتماد لم يمنحه أحد على
  مستند وجوده كله من أجل خطوة الاعتماد.

#### مسار السحب (المسار 4) — لماذا كان لا بد منه

قبل هذه المراجعة كان للحالة `pending_approval` مخرج واحد فقط: قرار المعتمِد المسمّى. والنتيجة أن
خطة أُرسلت إلى الشخص الخطأ — أو إلى شخص في إجازة أو غادر الشركة — تصبح **طريقاً مسدوداً دائماً**:
لا تُعدَّل (المحتوى مجمّد)، ولا تُحذف (الحذف للمسودة والمرفوضة فقط)، ولا يُستبدل معتمِدها، وسنتها
تبقى محجوزة عملياً. المسار 4 يفتح المخرج لصاحب الخطة وحده (ولمدير النظام)، والمعتمِد ليس منهما:
القرار مسؤولية يسجّلها، لا مهمة يتخلّص منها.

وحدوده مقصودة: `approverId` يجوز إفراغه أو تركه كما هو (`handleRecallPlan` يتركه ليكون الاختيار
الافتراضي عند إعادة الإرسال)، ولا يجوز تحويله إلى شخص آخر في نفس الكتابة — تغيير المعتمِد يمر
بالإرسال (المسار 2) حيث يُعاد التحقق من ألا يكون المعتمِد هو الكاتب.

#### مسح أختام الرفض (المساران 1 و 2)

الأختام كانت مجمّدة تماماً في وجه الكاتب، وهذا كان يُنتج تناقضاً: خطة تُرفض فتحمل
`rejectedBy/rejectedAt/rejectionReason`، ثم تُعدَّل وتُرسل وتُعتمد، فتصبح **معتمدة ومرفوضة في آن
واحد** بلا أي طريقة لتصحيحها من التطبيق. القواعد الآن تسمح للكاتب بأن **يمسح** الختم (قيمة فارغة
أو `null`) لا أن يكتب فيه قيمة. ما يكسبه الكاتب من ذلك هو محو رفضٍ لخطته هو، على خطة هو بصدد
إعادة إرسالها أصلاً؛ وتزوير اعتماد يبقى مستحيلاً، وهي الخاصية التي تهم.

#### المسار 5: كتابة `auditId` على بند الخطة

إنشاء مراجعة من بند مخطط يكتب معرّف المراجعة على البند (`linkAuditToPlanItem` في
`src/app/audits/new/page.tsx`)، ولا شيء آخر في النظام يحرّك نسبة إنجاز الخطة عن الصفر. كانت هذه
الكتابة **مرفوضة في 100% من الحالات** لأن الخطة تكون `approved` وقتها ولا فرع يطابقها.

الفرع الجديد مقيّد بندًا بندًا، لا بالثقة في الصفحة:

- على المستوى الأعلى لا يتحرك سوى `items` و `updatedAt`؛
- عدد البنود لا يتغيّر؛
- `removeAll` في الاتجاهين يجب أن يترك **بنداً واحداً على كل جهة** — البند قبل التعديل والبند بعده؛
- و `diff` بين هذين البندين يجب ألا يمس سوى `auditId`، وأن يكون فارغاً قبلها ونصاً غير فارغ بعدها.

فلا يمكن إضافة بند ولا حذف بند ولا تغيير إدارته أو شهره أو نوعه، ولا إعادة ربط بند مربوط أصلاً.
والبوابة `isActive()` — أي «من يستطيع إنشاء مراجعة» — لأن هذه الكتابة نتيجةٌ مباشرة لذلك الفعل.
(الواجهة أضيق: زر «إنشاء مراجعة من هذا البند» يظهر لموظفي الجودة وحدهم.)

> **حدّ معروف**: القاعدة تتحقق من الفرق بين قائمتَي البنود بالمقارنة البنيوية، وهي تعتمد على أن
> بنود الخطة تحمل معرّفات فريدة (`item.id`)، فلا يوجد بندان متطابقان تماماً. خطة كُتبت يدوياً
> ببندين متطابقين حرفياً ستُرفض عليها هذه الكتابة (لا تُقبل خطأً) — الرفض هو الاتجاه الآمن.

#### المساران 6 و 7: تعديل بند في خطة **معتمدة**

خطة سنوية تُعتمد في يناير وتُنفَّذ على مدى سنة: يستقيل مراجع، ويُنقل آخر، ويتعذّر شهر بسبب توقف
خط إنتاج. خطة لا تقبل التعديل بعد الاعتماد لا تُتَّبع — يُتجاوز عنها ويُدار العمل خارجها. لكن
«تقبل التعديل» ليست إذناً مفتوحاً، والتعديلان ليسا من نوع واحد، فالفصل بينهما هو **من يقرّر**:

| ما الذي يتغيّر | من يقرّر | كيف تفرضه القواعد |
| --- | --- | --- |
| `plannedMonth` | مدير الجودة وحده | يُكتب على البند مباشرة (المسار 6) |
| `pendingTeamChange` (اقتراح فريق) | مدير الجودة يرفعه أو يسحبه | يُكتب على البند مباشرة (المسار 6) |
| `leadAuditorId` و `auditorIds` | **معتمِد الخطة وحده** | لا يتحركان إلا في المسار 7 |

المسار 6 يسمح لـ `isQualityStaff()` بتغيير مفتاحين لا ثالث لهما على بند واحد: `plannedMonth` و
`pendingTeamChange`. أما `leadAuditorId` و `auditorIds` فليسا في مجموعة المفاتيح المسموحة أصلاً،
فصفحة الخطط **لا تستطيع** تغيير من يراجع مَن على خطة معتمدة مهما فعلت — الاقتراح يجلس بجانب
القيم السارية، وهي تبقى العاملة حتى يصدر القرار.

والمسار 7 هو القرار، وهو للمعتمِد المسمّى في `approverId` وحده — لا لمدير الجودة الذي طلبه، ولا
لمدير النظام — ومربوط بالمقترح الذي عُرض عليه فعلاً:

- يجب أن يكون البند **حاملاً** لـ `pendingTeamChange` قبل الكتابة؛
- ويجب ألا يحمله بعدها: المقترح يُستهلَك في الحالتين، اعتماداً كان أو رفضاً؛
- والفريق الناتج يجب أن يساوي **المقترح** (اعتماد) أو ما كان **سارياً** (رفض). أي شيء بينهما —
  اعتماد فريق لم يقترحه أحد — لا يطابق أياً منهما فيُرفض.

المسارات 5 و 6 و 7 تتشارك مقدمة واحدة (`oneApprovedLineMoved`): بند واحد يتحرك، بلا إضافة ولا
حذف ولا إعادة ترتيب. وتوحيدها ليس ترتيباً جمالياً بل ضرورة: Firestore يقيّم 1000 تعبير كحد أقصى
لكل طلب، وكل `removeAll` على قائمة البنود يكلّف مروراً على كل بند في الخطة. اشتقاق القائمتين
داخل كل فرع على حدة كان يعني خمسة عشر فرقاً على خطة قد تحمل خمسين بنداً، وتجاوز السقف يعود من
Firestore بصفة `PERMISSION_DENIED` — أي لا يمكن تمييزه عن رفض حقيقي. القائمتان تُشتقّان مرتين في
المقدمة ومرة في الوسائط، ولا تلمسهما الفروع بعد ذلك. (مُختبَر على المحاكي: كل الكتابات المشروعة
تمرّ على خطط بـ 12 و 40 و 80 بنداً.)

### 3.2 «خطة واحدة لكل سنة» ليست مفروضة في القواعد — بصراحة

الواجهة تمنع تكرار السنة (`plans.some(p => p.year === newPlan.year)` في `handleCreatePlan`)، وهذا
فحص عميل فقط: عمليتا إنشاء متزامنتان تنجحان معاً.

**ولا يمكن إغلاق ذلك في `firestore.rules` كما هي بنية البيانات اليوم.** السؤال "هل توجد خطة أخرى
بهذه السنة؟" يحتاج استعلاماً على المجموعة، وقواعد Firestore لا تستطيع سوى `get()` أو `exists()` على
مسار معلوم مسبقاً. لا تقرأ في أي مكان من هذه الوثيقة أن التفرد مضمون؛ ليس كذلك.

ما هو مفروض فعلاً:

- `year` يجب أن يكون رقماً بين 2000 و 2100 عند الإنشاء،
- و **مجمّد بعد الإنشاء**: لا يستطيع أي تعديل تحويل خطة سنة إلى نسخة ثانية من سنة أخرى.

**الحل الجذري إن أُريد ضماناً حقيقياً**: أن يكون معرّف المستند هو السنة نفسها
(`annualPlans/2026`) بدل `plan-${Date.now()}-...`؛ عندها يكفي `allow create: if !exists(...)`،
ويصبح التفرد خاصية في قاعدة البيانات لا اتفاقاً بين الصفحات. هذا يغيّر `createAnnualPlan` في
`src/lib/firestore.ts` ولم يُنفّذ في هذه المراجعة.

حتى ذلك الحين، الكاشف قائم: `getAnnualPlanByYear` يسجّل خطأً صريحاً عند وجود أكثر من خطة للسنة
الواحدة ويعيد الأحدث تحديثاً، فالتكرار يُلاحَظ ولا يمر صامتاً.

### 3.3 تفاصيل أخرى تستحق الانتباه

- **`authUsers`**: الكتابة مشروطة بأن يكون المستند بثلاثة حقول بالضبط، وأن يكون `role` أحد الأدوار
  الستة المعروفة، وأن يكون `isActive` قيمة منطقية، وأن **يوجد** `users/{userId}` وأن يكون دوره
  مطابقاً للدور المطلوب. هذا يمنع منح دور لا يظهر في دليل الموظفين. `saveUser` يكتب مستند المستخدم
  أولاً ثم مستند الربط، فالترتيب يجعل الشرط قابلاً للتحقق دائماً.
- **قراءة مستند ربط شخص آخر**: مسموحة لمن يملك **إصداره** — مدير النظام ومدير الجودة (القسم 2.2)
  — وهي ضرورية وليست ترفاً: `confirmAuthMapping` في `src/lib/auth.ts` يقرأ مستند الربط الذي كتبه
  للتو لموظف آخر ليتأكد أنه وصل الخادم فعلاً قبل إبلاغ المسؤول بالنجاح. وحين كانت القراءة محصورة
  بمدير النظام كان مدير الجودة يُنشئ الموظف ويُصدر حساب دخوله فتفشل خطوة التأكيد، **فيُحذف حساب
  المصادقة المُنشأ للتو** ويُبلَّغ بأن العملية فشلت — أي أن صلاحية منحها له القسم 2.2 كانت معطّلة
  عملياً. أما `list` فبقي لمدير النظام وحده: المسموح قراءة مستند مُسمّى، لا تعداد المجموعة.
- **`audits`**: `subscribeToAudits` مستمع على المجموعة كاملة، فأي شرط يعتمد على محتوى المستند كان
  سيُفشل المستمع كله ويُفرغ لوحة المعلومات وصفحات الملاحظات والمتابعة. لذلك القراءة والتعديل
  مفتوحان لكل موظف نشط عمداً.
- **`notifications`**: كل استعلام في التطبيق مقيّد إمّا بـ `where('recipientId', '==', <هويتي>)`
  وإمّا بـ `where('senderId', '==', <هويتي>)`، وهو ما يجعل قاعدة القراءة قابلة للتحقق على مستوى
  الاستعلام. أي استعلام جديد بلا أحد القيدين سيُرفض — وهذا مقصود.

  شرط المُرسِل أُضيف لسبب واحد: **حذف الخطة السنوية يجب أن يأخذ معه إشعاراتها**. الإشعار يظهر في
  جرس رأس الصفحة، أي في كل صفحة، فبقاء «خطة سنوية بانتظار اعتمادك» بعد حذف الخطة يعني مطالبة
  دائمة باعتماد شيء لم يعد موجوداً، والضغط عليها يفتح `/plans?plan=<محذوفة>` فلا يحدث شيء.
  ومعتمِد الخطة ليس شخصاً ثابتاً — مدير الجودة يختاره من قائمة المستخدمين على كل خطة — فطلب
  الاعتماد مُرسَل إلى شخص ليس هو مدير الجودة، وبشرط المستقبِل وحده كان مدير الجودة يحذف الخطة ولا
  يستطيع حذف الإشعار الذي أرسله عنها. التوسعة ضيّقة ومتناظرة: تسحب إشعاراً أرسلتَه أنت، كما تُزيل
  إشعاراً وصل إليك. ولا تفتح شيئاً بين طرفين آخرين: `senderId` يُكتب عند الإنشاء ولا يتغيّر بعدها
  أبداً لأن قاعدة التعديل لا تسمح إلا بالحقل `read`، فلا يمكن توجيهه لاحقاً إلى مراسلات غيرك.
  المنفّذ هو `deleteNotificationsForPlan` في `src/lib/firestore.ts`، ويستدعيه `confirmDeletePlan`
  في `src/app/plans/page.tsx` بعد نجاح حذف الخطة، ويُسجَّل الحذف في سجل النشاط (القسم 2.3).

### 3.4 `activeSessions` مغلقة — والمستندات الباقية بيانات ميتة

ميزة «من متصل الآن» أُزيلت. قائمة النوافذ المفتوحة لم تكن تجيب عن سؤال للعمل، ونبضة الإبقاء
(heartbeat) التي تحتاجها كانت تكتب في Firestore لكل مستخدم مسجَّل الدخول طوال اليوم. البديل هو سجل
النشاط (القسم 2.3): ما فعله الناس، لا أي النوافذ مفتوحة.

`addActiveSession` و `removeActiveSession` و `getActiveSessions` و `updateSessionActivity` و
`subscribeToActiveSessions` و `cleanupStaleSessions` لم تعد موجودة في `src/lib/firestore.ts`، ولا
شاشة تقرأ المجموعة أو تكتب فيها. لذلك القاعدة صارت `allow read, write: if false;` — وهي **لا تمنع
شيئاً يستدعيه التطبيق اليوم**. أُغلقت المجموعة ولم تُحذف من الملف عمداً: أي مستدعٍ متبقٍّ يُرفض هنا
بصراحة بدل أن يسقط في قاعدة عامة لا تصفه.

**المستندات الموجودة في المجموعة لم تُمسّ.** هي بيانات ميتة — لقطة لمن كانت نافذته مفتوحة يوم
إزالة الميزة — ولم يعد أحد يقرؤها ولا يحدّثها. **للمشغّل أن يحذف المجموعة من Firebase Console متى
شاء، ولا شيء يستعجله ولا شيء يعتمد عليها.** (القواعد لا تسري على Console، فالإغلاق هنا لا يمنع
الحذف من هناك.)

> ⚠️ **أثر واحد على النسخ الاحتياطي، وهو المهم عملياً**: `scripts/backup-firestore.mjs` ما زال
> يُدرج `activeSessions` في قائمة `COLLECTIONS`، والسكربت يعمل بـ Client SDK تحت هذه القواعد
> نفسها. فمنذ نشر هذه المراجعة سيطبع السطر `activeSessions FAILED: ...` ثم
> `INCOMPLETE` وينهي بالرمز `1` **في كل مرة**، رغم أن النسخة كاملة فعلاً. وهذا يخالف الخطوة 9 من
> قائمة التحقق (القسم 7) التي تشترط الرمز `0`.
> **الإصلاح في السكربت لا في القواعد**: احذف `'activeSessions'` من `COLLECTIONS`، تماماً كما حُذفت
> `'passwords'` منها وللسبب نفسه المشروح في تعليق السكربت. وأضف `'activityLog'` مكانها — فهي
> مجموعة حيّة يقرؤها مدير النظام، وغيابها عن القائمة يعني نسخة احتياطية بلا سجل النشاط. حتى يُنفَّذ
> ذلك، اقرأ الرمز `1` مع سطر `FAILED` يخصّ `activeSessions` وحدها على أنه نجاح، وأي مجموعة أخرى في
> السطر نفسه على أنه فشل حقيقي.

---

## 4. نافذة الترحيل القديمة (قراءة `users` و `passwords` بلا مصادقة): مغلقة نهائياً

> هذا القسم عن النافذة **القديمة** الواسعة التي كان يحتاجها الدخول بكلمة المرور القديمة. أما
> النافذة الأولية الضيقة التي تفتحها القواعد اليوم لإنشاء أول مدير نظام، فهي القسم 5.0 — ولا
> تتقاطع مع هذه في شيء: لا تقرأ `passwords`، ولا تسرد `users`.

### 4.1 ما الذي كان يحتاجها

الترحيل الكسول (`migrateLegacyUser` سابقاً في `src/lib/auth.ts`) كان يعمل قبل وجود أي حساب Auth،
أي **بدون مصادقة**، ويحتاج شيئين:

1. البحث عن المستخدم بالبريد في `users`.
2. قراءة `passwords/{userId}` للتحقق من كلمة المرور القديمة.

### 4.2 لماذا لم يكن ممكناً تضييقها

- قواعد Firestore **لا ترى شرط `where`** في الاستعلام. السماح بالبحث بالبريد يعني حتماً السماح
  بسرد مجموعة `users` كاملة: أسماء وبُرُد وهواتف وأدوار كل الموظفين، لأي زائر.
- معرّف مستند كلمة المرور **هو نفسه** معرّف مستند المستخدم (`passwords/{userId}`). فبمجرد أن يصبح
  سرد `users` متاحاً للمجهول، تصبح كل معرّفات كلمات المرور معلومة، ويصبح "السماح بـ `get` فقط" بلا
  معنى: حلقة واحدة تكفي لجمع كل التجزئات.
- بعض القيم المخزّنة **ليست تجزئات أصلاً**: `verifyPassword` في `src/lib/firestore.ts` يتعامل مع
  قيم نصية قديمة صريحة (plaintext) بالمقارنة المباشرة. أي أن النافذة كانت تكشف كلمات مرور فعلية،
  لا تجزئات bcrypt فقط.

كان هذا تناقضاً مغلقاً: القواعد المشدّدة تُفشل الترحيل، والترحيل يطالب بفتح القواعد. حُلّ التناقض
بحذف الطرف الذي لا يمكن تأمينه.

### 4.3 القرار: حذف المسار القديم، والاستعاضة عنه بتمكين إداري

**لم يعد في الشيفرة أي مسار دخول قديم.** `signIn` في `src/lib/auth.ts` خطوتان لا ثالث لهما:
مصادقة Firebase، ثم قراءة مستند الربط. بريد بلا حساب Auth هو `invalid_credentials`، وحساب بلا
مستند ربط هو `not_linked` وتُنهى جلسته فوراً. ولم يعد في `resolveAppUser` أي بحث احتياطي بالبريد:
كتابة مستند ربط لنفسك هي بالضبط ما ترفضه القواعد، فذلك المسار كان لا يستطيع إلا الفشل — وكان يبلّغ
عن فشله بأنه "لا يوجد مستند ربط".

المقايضة بصراحة:

| ما خسرناه | ما ربحناه |
| --- | --- |
| لم يعد بإمكان موظف قديم أن يدخل بكلمة مروره القديمة ويُنشئ حسابه تلقائياً | لا دليل موظفين مكشوف، ولا تجزئات كلمات مرور، ولا كلمات مرور صريحة لأي زائر |
| على مدير النظام تمكين كل موظف قائم مرة واحدة من صفحة المستخدمين (زر واحد لكل موظف) | لا يوجد تاريخ انتهاء يجب تذكّره، ولا "نافذة مؤقتة" تُنسى مفتوحة كما نُسيت قاعدة وضع الاختبار |

**استثناء واحد باقٍ من الوصول المجهول، وهو ليس هذا المسار**: لوحة «الإعداد الأولي» في
`src/app/login/page.tsx` تقرأ مستند `users/system-admin-root` وحده، بدون مصادقة، وما دام بلا
`authUid`. هذه هي النافذة الأولية بكل تفاصيلها ومخاطرها في **القسم 5.0**، ولا علاقة لها بمجموعة
`passwords`: الفحص لم يعد يقرؤها إطلاقاً.

### 4.4 مجموعة `passwords`: خاملة — ومتى يحذفها المشغّل

القواعد الآن على هذه المجموعة:

```
allow read: if false;                // لا أحد، ولا حتى مدير النظام
allow create, update: if false;      // لا أحد
allow delete: if isQualityStaff();   // فقط ليعمل حذف المستخدم
```

**لماذا الحذف وحده مسموح**: `deleteUser` في `src/lib/firestore.ts` يحذف `users/{id}` ثم
`passwords/{id}` في نفس الاستدعاء. لو مُنع الحذف لفشل الاستدعاء **بعد** أن يكون المستخدم قد حُذف
فعلاً، ولأبلغ صفحة المستخدمين بفشل عملية نجحت. والحذف يُتلف تجزئة ولا يكشفها، وبوابته هي نفس بوابة
مستند المستخدم — ولذلك اتّسعت معها إلى `isQualityStaff()` حين صار حذف المستخدم بيد مدير الجودة
أيضاً (القسم 2.2): إبقاؤها على `isAdmin()` كان سيعيد إنتاج العيب نفسه الموصوف في هذه الفقرة، في كل
عملية حذف يجريها مدير الجودة. (حذف مستند غير موجود في Firestore عملية ناجحة لا تفعل شيئاً، فبقاء الاستدعاء بعد
إفراغ المجموعة سليم.)

**الدوال التي كانت تصل إلى المجموعة كلها تفشل بأمان الآن**: `getPassword` تُعيد `null`،
`hasPassword` تُعيد `'unknown'` (وكل مستدعييها يعاملونها على أنها "لا تتصرف")، `verifyPassword`
تُعيد `false`، و `setPassword` / `resetPassword` تُعيدان `false`. ولا شيء من ذلك على مسار دخول.

**النسخة الاحتياطية لا تستطيع تصديرها**: `scripts/backup-firestore.mjs` يعمل بحزمة العميل تحت هذه
القواعد نفسها، فأي محاولة لتصدير `passwords` كانت ستُنتج فشلاً مضموناً ورمز خروج `1` ونسخة تبدو
ناقصة وهي كاملة. لذلك أُخرجت المجموعة من قائمة `COLLECTIONS` في السكربت وأُضيفت `annualPlans` التي
كانت ناقصة فعلاً. **إن أردت نسخة من التجزئات القديمة فخذها من Firebase Console** — القواعد لا
تُطبَّق على Console ولا على Admin SDK — أو من نسخة `gcloud firestore export` إن توفرت.

**متى تحذفها؟** بهذه الشروط الثلاثة، ولا يلزم أكثر منها:

1. أن تكون القواعد الجديدة **منشورة** فعلاً (وإلا فالمجموعة ما زالت مقروءة من العميل).
2. أن تكون قد أخذت نسخة منها من Console **إن كنت تريد نسخة أصلاً**. الرجوع إلى نظام الدخول القديم
   لم يعد ممكناً على أي حال: شيفرته حُذفت، فقيمة هذه النسخة أرشيفية بحتة.
3. أن تكون نسخة `backup-firestore.mjs` الأخيرة قد نجحت برمز خروج `0` (لتأمين بقية البيانات، لا
   لتأمين التجزئات).

ولا يلزم انتظار تمكين كل الموظفين: لا شيء في التطبيق يقرأ هذه المستندات، فحذفها لا يعطّل مسار
دخول أحد. الأفضل حذفها مبكراً — أفضل حماية لتجزئة كلمة مرور هي ألا تكون موجودة.

> ⚠️ `scripts/check-auth-migration.mjs --check-legacy-passwords` يقرأ `passwords` مباشرة، وهو الآن
> مرفوض دائماً: الخيار يُنهي التقرير برمز خروج `2` قبل طباعته. **شغّل السكربت بدون هذا الخيار.**

---

## 5. تمكين الدخول: النافذة الأولية، ثم التمكين الإداري

### 5.0 النافذة الأولية (Bootstrap) — اقرأ هذا القسم كاملاً قبل النشر

#### لماذا توجد أصلاً

المشروع على خطة Spark: لا `firebase-admin`، ولا حساب خدمة، ولا Cloud Functions. أي أن **كل** ما
يُكتب في قاعدة البيانات يُكتب من متصفح، تحت هذه القواعد نفسها. وقاعدة لا تسمح بأي مطالبة ذاتية
إطلاقاً لا يمكن إقلاعها إلا من Firebase Console. لذلك تفتح القواعد نافذة واحدة، محدودة، **تُغلق
نفسها**، ليتم الإقلاع من داخل التطبيق.

#### ما الذي تسمح به بالضبط — ثلاث سماحات، لا أكثر

| # | العملية | الشروط |
| --- | --- | --- |
| أ | `get` **بدون مصادقة** على `users/system-admin-root` | هذا المعرّف وحده. `get` فقط — **لا `list`**، فمجموعة `users` تبقى مغلقة. ومشروطة بألا يحمل المستند `authUid` |
| ب | `create` (لا `update`) على `authUsers/{uid}` من صاحب الحساب نفسه | `request.auth.uid == {uid}`، والحمولة **بالضبط** `{ userId: 'system-admin-root', role: 'system_admin', isActive: true }` بثلاثة حقول لا رابع لها، ولا يوجد مستند ربط لهذا الـ UID، **وألا يحمل** `users/system-admin-root` حقل `authUid` |
| ج | `update` على `users/system-admin-root` يكتب `authUid` | أن يكون لدى المتصل مستند ربط يشير إلى `system-admin-root`، وألا يكون الحقل مكتوباً أصلاً، وألا تُمس أي حقول أخرى، وأن تكون القيمة هي UID المتصل نفسه |

#### كيف تُغلق نفسها

السماحات الثلاث كلها مشروطة بشيء واحد: **`users/system-admin-root` لا يحمل `authUid`**. والسماحة
(ج) هي التي تكتب هذا الحقل، وهي آخر خطوة في `bootstrapSystemAdmin` (`src/lib/auth.ts`) — بعد
إنشاء الحساب، وبعد كتابة مستند الربط، وبعد **إعادة قراءته من الخادم للتأكد**. فبمجرد نجاح
التهيئة: تُرفض القراءة المجهولة، وتُرفض أي مطالبة ذاتية أخرى، ولا يبقى للسماحة (ج) ما تكتبه.
النافذة **لا يمكن إعادة فتحها من التطبيق** — مسح `authUid` يحتاج Console أو Admin SDK، وقواعد
Firestore لا تُطبَّق على أيٍّ منهما.

وإن فشلت التهيئة في منتصفها، تحذف الدالة مستند الربط ثم حساب Auth الذي أنشأته، فتبقى قاعدة
البيانات كما كانت وتبقى النافذة صالحة لمحاولة أخرى. وإن تعذّر حذف أحدهما فالرسالة تسمّي ما بقي —
لا تتجاهلها.

#### الخطر — بصراحة تامة

> ⚠️ **بين لحظة نشر هذه القواعد ولحظة إتمام التهيئة، مَن يصل إلى `/login` أولاً يملك حساب مدير
> النظام.** ليس تنظيراً: التسجيل الذاتي مفعّل، وبريد مدير النظام مكتوب في الحزمة المنشورة
> (`SYSTEM_ADMIN_EMAIL` في `src/lib/firestore.ts`)، فيكفي أي شخص أن يفتح الصفحة ويكتب كلمة مرور
> مرتين ليصبح `system_admin` — وتُغلق النافذة خلفه، ولا تستطيع أنت استعمالها بعده.

وما تكشفه النافذة وهي مفتوحة: **مستند واحد** — بطاقة مدير النظام في دليل الموظفين (الاسم بالعربية
والإنجليزية، البريد، الرقم الوظيفي، المسمى الوظيفي، الهاتف، الدور). لا الدليل، ولا أي موظف آخر،
ولا شيء قابل للكتابة. هذا ثمن الإقلاع البارد من عميل بلا خادم.

#### التزام المشغّل

1. **انشر القواعد وأتمم التهيئة في الجلسة نفسها.** لا تنشر مساءً لتُكمل صباحاً.
2. لا تُعلن عن عودة النظام قبل إتمام التهيئة.
3. افتح `https://<الموقع>/login` فور النشر. إن ظهرت لوحة «الإعداد الأولي» فاكتب كلمة مرور قوية
   مرتين، ثم سجّل الدخول بالبريد نفسه.
4. **تحقّق فوراً**: Firestore → `users/system-admin-root` يجب أن يحمل الآن `authUid`، و
   `authUsers/{ذلك الـ UID}` يجب أن يكون موجوداً بثلاثة حقول. عندها تكون النافذة مغلقة.
5. **إن لم تظهر اللوحة** فالسبب أحد ثلاثة: التهيئة تمّت فعلاً (تحقّق من `authUid` كما في 4)، أو
   `users/system-admin-root` غير موجود (فالمسار الوحيد عندها هو القسم 5.1)، أو القواعد لم تُنشر
   بعد. الصفحة **لا تعرض اللوحة عند الشك** عمداً: عرضها على غير يقين يمنح أي زائر الحساب.
6. **إن وجدت النافذة مغلقة ولم يقم أحد من فريقك بالتهيئة — عاملها كاختراق**: احذف مستند `authUsers`
   الغريب وحساب Auth المقابل من Console، وامسح `authUid` من `users/system-admin-root`، ثم أعد
   الكرّة فوراً.

#### شرط مسبق واجب التحقق

النافذة تفترض **وجود** مستند `users/system-admin-root`: السماحتان (أ) و (ب) تقرآنه، والسماحة (ج)
تعدّله، ولا واحدة منها تستطيع إنشاءه. والدالة `initializeSystemAdmin` التي كانت تُنشئه تحتاج اليوم
صلاحية مدير نظام (إنشاء مستند في `users` مقصور على `isAdmin()`)، فلا تستطيع إنشاءه قبل التهيئة.
تحقّق من وجوده من Console قبل النشر؛ إن لم يكن موجوداً فلا مفر من الخطوات اليدوية في القسم 5.1.

ولاحظ فرقاً دقيقاً: القاعدة **تثبّت** الدور المكتوب في مستند الربط على `system_admin` نصاً، ولا
تقارنه بدور مستند المستخدم (بخلاف الكتابة العادية لمستندات الربط التي تشترط التطابق). فاحرص أن
يحمل `users/system-admin-root` الدور `system_admin` أيضاً، وإلا صار الدليل يقول شيئاً وسجل
الصلاحيات شيئاً آخر — وهو ما يربك `check-auth-migration.mjs` وأي مراجعة لاحقة.

### 5.1 المسار البديل — إنشاء أول مستند ربط يدوياً من Firebase Console

استعمل هذا القسم إذا لم تنجح النافذة الأولية (5.0): مستند `users/system-admin-root` غير موجود، أو
النافذة أُغلقت، أو تفضّل ألا تفتحها أصلاً. نفّذ هذه الخطوات بنفسك، من حساب مالك المشروع، قبل أن
تخبر أحداً بأن النظام عاد للعمل.

> ملاحظة: تنفيذ هذه الخطوات **يغلق النافذة الأولية** أيضاً، لأن الخطوة 4 تكتب `authUid` على مستند
> مدير النظام. وهذا مقصود: مسار واحد يكفي.

1. **Firebase Console → Authentication → Sign-in method**: تأكد أن Email/Password مفعّلة.
2. **Authentication → Users → Add user**: أنشئ حساباً ببريد مدير النظام. البريد المتوقّع في
   الشيفرة هو الثابت `SYSTEM_ADMIN_EMAIL` في `src/lib/firestore.ts`. اختر كلمة مرور قوية (6 أحرف
   على الأقل — `MIN_PASSWORD_LENGTH` في `src/lib/auth.ts`).
3. **انسخ الـ UID** الظاهر أمام الحساب في القائمة. هذا هو `{authUid}`.
4. **Firestore → مجموعة `users` → المستند `system-admin-root`**: تأكد من وجوده. إن لم يكن موجوداً
   فأنشئه بهذا المعرّف بالضبط، بالحقول التالية (نفس قيم الثابت `SYSTEM_ADMIN` في
   `src/lib/firestore.ts`):

   | الحقل | النوع | القيمة |
   | --- | --- | --- |
   | `id` | string | `system-admin-root` |
   | `employeeNumber` | string | `SYS-0001` |
   | `email` | string | نفس بريد الخطوة 2، بحروف صغيرة |
   | `fullNameAr` | string | `مدير النظام` |
   | `fullNameEn` | string | `System Administrator` |
   | `role` | string | `system_admin` |
   | `departmentId` | string | `` (فارغ) |
   | `sectionId` | string | `` (فارغ) |
   | `canBeAuditor` | boolean | `true` |
   | `auditableDepartmentIds` | array | `[]` |
   | `auditableSectionIds` | array | `[]` |
   | `phone` | string | `+966500000000` |
   | `jobTitleAr` | string | `مدير النظام` |
   | `jobTitleEn` | string | `System Administrator` |
   | `isActive` | boolean | `true` |
   | `isSystemAccount` | boolean | `true` |
   | `createdAt` | string | تاريخ ISO، مثل `2024-01-01T00:00:00.000Z` |
   | `updatedAt` | string | نفس القيمة |
   | `authUid` | string | **الـ UID من الخطوة 3 — مطلوب، انظر أدناه** |

   > ⚠️ **حقل `authUid` مطلوب وليس اختيارياً.** نسخة سابقة من هذه الوثيقة وصفته بأنه "اختياري
   > ومستحسن"، وهذا خطأ له أثر عملي: `saveUser` في `src/lib/firestore.ts` لا يزامن مستند الربط
   > (`role` و `isActive`) إلا إذا وجد `authUid` على مستند المستخدم — الشرط `if (authUid)` يتخطى
   > المزامنة كلها بدونه. النتيجة بدون هذا الحقل: تعديل دور مدير النظام أو تعطيله من صفحة
   > المستخدمين يُكتب على مستند المستخدم ولا يصل إلى مستند الربط، والقواعد تقرأ الربط وحده، فيبقى
   > صاحب الحساب بصلاحياته القديمة. (`scripts/check-auth-migration.mjs` يعتبره "نصف مرتبط" أيضاً،
   > لكن هذا أهون الأثرين.)

5. **Firestore → أنشئ مجموعة `authUsers` → مستند بمعرّف = الـ UID من الخطوة 3**، بثلاثة حقول
   **بالضبط**:

   | الحقل | النوع | القيمة |
   | --- | --- | --- |
   | `userId` | string | `system-admin-root` |
   | `role` | string | `system_admin` |
   | `isActive` | boolean | `true` |

   > ⚠️ ثلاثة أخطاء شائعة تُفشل الدخول كلها بصمت: كتابة `isActive` كنص `"true"` بدل قيمة منطقية،
   > أو إضافة حقل رابع (القاعدة تشترط ثلاثة حقول بالضبط عند الكتابة من التطبيق)، أو دور لا يطابق
   > `role` على مستند المستخدم.

6. **انشر القواعد** (القسم 6)، ثم سجّل الدخول من التطبيق ببريد وكلمة مرور الخطوة 2.

### 5.2 تمكين موظف قائم — رمز وصول يُنطق، لا رسالة بريد

هذا هو المسار الوحيد لكل موظف موجود في قاعدة البيانات وليس له حساب دخول. لا يحتاج كلمة مروره
القديمة، ولا يحتاج أي قراءة مجهولة، لأن **المسؤول مُصادق عليه أصلاً** وكل الكتابات تتم بجلسته.
وبعد توسعة القسم 2.2 يستطيع **مدير إدارة الجودة** أن يقوم به كاملاً، لا مدير النظام وحده.

> **لماذا رمز وصول ولا بريد**: لا توجد في هذا المشروع خدمة بريد عاملة، فرسالة تعيين كلمة المرور لا
> تصلح أن تكون طريق أحد إلى النظام. البديل: يُنشأ الحساب **برمز وصول لمرة واحدة** يظهر للمسؤول على
> الشاشة **مرة واحدة**، يُملى على الموظف مباشرة (مشافهةً أو على الهاتف)، ويستبدله الموظف بكلمة مرور
> من عنده عند أول دخول.

من صفحة المستخدمين، على أي مستخدم لا يحمل `authUid`، اضغط **«إنشاء حساب دخول»**. ما يحدث خلفها
(`createSignInAccountForUser` في `src/lib/auth.ts`):

1. يُولَّد **رمز الوصول** (`generateAccessCode` في `src/lib/firestore.ts`): 8 رموز من أبجدية
   منقّاة من الأحرف الملتبسة (لا `0/O` ولا `1/I/L`) في مجموعتين تُقرأان بسهولة مثل `K7RM-P2XD`،
   بأخذ عيّنات بالرفض (rejection sampling) لا بباقي القسمة، فتتساوى احتمالات كل الرموز — نحو 39.6
   بت. ويُستعمل هذا الرمز **كلمةَ مرور الحساب عند إنشائه** على **النسخة الثانوية** من تطبيق
   Firebase، فلا تُستبدل جلسة المسؤول.
2. يُكتب `authUsers/{uid}` (سجل الصلاحيات) و `users/{id}.authUid` (المؤشر العكسي) — بجلسة المسؤول،
   وهو ما تشترطه القواعد — ثم **يُقرأ مستند الربط من الخادم للتأكد** قبل الإبلاغ بالنجاح. حساب بلا
   مستند ربط هو حساب مُصادق عليه وممنوع من كل شيء، وإعلانه نجاحاً يعطي المسؤول مهمة تبدو مكتملة
   ويعطي الموظف دخولاً معطّلاً.
3. يُضبط `users/{id}.mustChangePassword = true` (`setMustChangePassword`). **هذه الخطوة هي ما يمنع
   رمز الوصول من أن يصير كلمة مرور دائمة**: `checkPendingPasswordChange` ترفض فتح أي جلسة تطبيق ما
   دامت العلامة قائمة، فلا يملك حاملُ الرمز إلا شيئاً واحداً — أن يستبدله بكلمة مرور من عنده. ولو
   فشلت هذه الخطوة **يُحذف الحساب كله ويُتراجع عن كل شيء** (`rollbackCreatedAccount`)، لأن حساباً
   برمز يعمل إلى الأبد أسوأ من لا حساب.
4. يُسجَّل الخروج من النسخة الثانوية في `finally`، فلا تبقى جلسة الحساب الجديد معلّقة مهما فشلت
   الخطوات السابقة.
5. يُعاد الرمز إلى الصفحة لتعرضه **مرة واحدة**. **الرمز لا يُخزَّن في أي مكان**: لا في Firestore،
   ولا في حقل على مستند المستخدم، ولا في سجل. تخزينه كان سيعيد إنتاج مشكلة `passwords` نفسها —
   قيمة يقرؤها من يقرأ المجموعة وتظل صالحة إلى الأبد.

**وماذا يفعل الموظف؟** يفتح `/login`، يكتب بريده والرمز، فتظهر له شاشة اختيار كلمة المرور
الإلزامية. ثم `setPasswordWithAccessCode` (`src/lib/auth.ts`) يسجّل دخوله بالرمز على النسخة
الثانوية — وهو نفسه إعادة المصادقة التي يطلبها Firebase قبل `updatePassword`، وأحدثها ممكنة — ثم
يضبط كلمة المرور الجديدة. وأخيراً يدخل بجلسته العادية **فيمسح العلامة عن مستنده هو**.

> **وهذا يحتاج قاعدة، وهي مضافة**: مسح `mustChangePassword` كتابة لا يستطيعها إلا صاحب المستند
> نفسه، وقاعدة `users` قبل هذه المراجعة كانت تمنع الموظف العادي من الكتابة على مستنده إطلاقاً — أي
> أن **الخطوة الأخيرة من كل تمكين كانت سترفض**. القاعدة المضافة (`clearsOwnPasswordChangeFlag` في
> `firestore.rules`) مقيّدة بأضيق ما يمكن: مستند صاحبها وحده، والحقول الثلاثة التي تكتبها
> `setMustChangePassword` فقط (`mustChangePassword`, `onboardedAt`, `updatedAt`)، و**المسح فقط لا
> الضبط** — فرض العلامة على أحد فعلُ مسؤول، ورفعها عن نفسك إنهاءُ الموظف لتمكينه.
> **وما لا تثبته**: القاعدة لا ترى كلمة مرور Auth، فهي تشهد أن الموظف مسح العلامة لا أنه استبدل
> الرمز فعلاً. من ينادي الدالة مباشرة بدل استعمال الشاشة يستطيع إبقاء الرمز كلمةً لمروره. هذا
> اختيار شخص لكلمة مرور ضعيفة **لحسابه هو**، وليس طريقاً إلى حساب غيره، ولا يمكن إغلاقه من العميل.
> والترتيب مقصود: تتغيّر كلمة المرور أولاً، ثم تُفتح الجلسة، ثم تُمسح العلامة. إن أُغلقت النافذة في
> المنتصف بقيت العلامة، وبطل الرمز، وعاد الدخول بكلمة المرور الجديدة إلى الخطوة نفسها.

الحالات التي تُبلَّغ بوضوح بدل أن تُخمَّن: `no_email` (لا بريد للموظف)، `inactive` (موظف معطّل — لا
يُنشأ له حساب)، `flag_not_set` (تعذّر ضبط العلامة، فأُلغي الحساب كله)، و `account_exists` (بريده
يحمل حساب Auth بالفعل).

**الموظف الذي فقد رمزه قبل استعماله**: `issueNewAccessCodeForUser` هو نفسه
`createSignInAccountForUser`، فيعمل **فقط** إن لم يكن للموظف حساب Auth (مستنده بلا `authUid`، أو
حُذف حسابه من Console). أما إن كان الحساب قائماً فيعيد `account_exists` **ولا يتغيّر شيء**: تعيين
كلمة مرور لحساب قائم يحتاج Admin SDK أو Console، وحزمة العميل لا تستطيع إلا تعيين كلمة مرور الحساب
المسجَّل دخوله بها. المخرجان الوحيدان: رابط إعادة التعيين بالبريد (إن كان بريد الموظف يعمل فعلاً)،
أو حذف حساب Auth من Console ثم الضغط على «إنشاء حساب دخول» من جديد.

**كلمة المرور المنسية بعد التمكين**: زر "نسيت كلمة المرور؟" في صفحة الدخول، أو إرسال الرسالة من
صفحة المستخدمين (`resetUserPassword` في `src/contexts/AuthContext.tsx`) — وهي ترفض الإرسال لمستخدم
بلا `authUid` بدل أن تدّعي النجاح، لأن Firebase مع حماية تعداد الحسابات "ينجح" صامتاً على بريد لا
حساب له. **وهذا المسار وحده ما زال معلّقاً على البريد** — انظر القسم 8، البند 15.

---

## 6. النشر

`firebase.json` في جذر المستودع يشير إلى `firestore.rules`، ولا يوجد `.firebaserc` (فقط
`.firebaserc.example`)، لذلك `firebase deploy` وحده يفشل بـ "No currently active project":

```bash
npm install -g firebase-tools                                   # مرة واحدة
firebase login                                                  # مرة واحدة
firebase deploy --only firestore:rules --project <projectId>
```

`<projectId>` هو `NEXT_PUBLIC_FIREBASE_PROJECT_ID` من `.env.local`. تشغيل `firebase use --add` مرة
واحدة يكتب `.firebaserc` ويغنيك عن `--project` لاحقاً.

بعد النشر، تحقّق من القواعد المنشورة من Console (Firestore → Rules) وقارنها بالملف — النشر من جهاز
آخر أو من فرع آخر يمكن أن يكون قد سبقك.

---

## 7. قائمة تحقق المشغّل

بالترتيب. لا تقفز خطوة.

1. ☐ اقرأ هذه الوثيقة كاملة، وبالذات القسمين 4 و 5 (والقسم 5.0 قبل النشر بدقيقة).
2. ☐ راجع `firestore.rules` بنفسك. لا تنشر قواعد لم تقرأها.
3. ☐ إن أردت أرشفة تجزئات `passwords` فصدّرها **الآن** من Firebase Console، قبل النشر أو بعده
   (Console لا تخضع للقواعد) — السكربت لن يستطيع ذلك بعد اليوم. القسم 4.4.
4. ☐ تحقّق من Console أن `users/system-admin-root` موجود وأن `role` فيه `system_admin`. إن لم يكن
   موجوداً فالمسار هو القسم 5.1 اليدوي، لا النافذة الأولية.
5. ☐ **افرغ وقتاً للخطوتين 6 و 7 معاً.** ما بينهما هو النافذة المفتوحة (القسم 5.0)، ومن يصل إلى
   `/login` قبلك يملك حساب مدير النظام.
6. ☐ انشر القواعد (القسم 6).
7. ☐ افتح `/login` **فوراً**: اكتب كلمة مرور قوية مرتين في لوحة «الإعداد الأولي»، ثم سجّل الدخول
   بالبريد نفسه. إن لم تظهر اللوحة فراجع القسم 5.0 (الالتزام رقم 5) قبل أي شيء آخر.
8. ☐ تأكد أن النافذة أُغلقت: `users/system-admin-root` يحمل `authUid`، ويقابله مستند
   `authUsers/{uid}` بثلاثة حقول. إن فشل الدخول، انظر القسم 9 قبل تغيير أي قاعدة.
9. ☐ خذ أول نسخة احتياطية صالحة:
   `QMS_OPERATOR_EMAIL=... QMS_OPERATOR_PASSWORD='...' node scripts/backup-firestore.mjs`
   ورمز الخروج يجب أن يكون `0`. الرمز `1` يعني نسخة ناقصة وليست نقطة استرجاع.
   **استثناء مؤقت واحد**: ما دامت `'activeSessions'` مذكورة في قائمة `COLLECTIONS` داخل السكربت،
   سيكون الرمز `1` دائماً بسبب تلك المجموعة وحدها — وهي مغلقة عمداً وفارغة المعنى. اقرأ سطور
   `FAILED`: إن كانت `activeSessions` وحدها فالنسخة سليمة. الإصلاح وشرحه في القسم 3.4.
10. ☐ من صفحة المستخدمين: أنشئ حساب دخول لمدير إدارة الجودة، ثم لبقية الموظفين، بزر «إنشاء حساب
    دخول» (القسم 5.2). كل موظف سيصله بريد ليختار كلمة مروره بنفسه — لا تُرسل كلمات مرور.
    **ابدأ بمدير إدارة الجودة**: بعد هذه المراجعة يستطيع هو أن يُكمل تمكين بقية الموظفين بنفسه
    (القسم 2.2)، فلا يبقى إنشاء المستخدمين معلّقاً عليك وحدك.
11. ☐ شغّل `node scripts/check-auth-migration.mjs` (**بدون** `--check-legacy-passwords`) وتأكد أن
    كل مستخدم له `authUid` ومستند ربط مطابق، وأنه لا توجد مستندات ربط يتيمة.
12. ☐ احذف مجموعة `passwords` من Console متى تحققت شروط القسم 4.4 الثلاثة.
13. ☐ عند تعطيل موظف: غيّر `isActive` إلى `false` من صفحة المستخدمين. هذا وحده يقطع وصوله فوراً عند
    أول عملية، لأن القواعد تقرأ `isActive` من مستند المستخدم مباشرة (القسم 2) — ولم يعد ذلك مشروطاً
    بوجود `authUid` كما كان. عند مغادرته نهائياً: احذف المستخدم، ثم احذف حساب Auth الخاص به من
    Console ومستند `authUsers` المقابل — انظر القسم 8.
14. ☐ راجع مجموعة `authUsers` دورياً: كل مستند فيها هو ترخيص دخول قائم.

---

## 8. ما الذي ما زال غير محمي — بصراحة

1. **المراجعات مقروءة لكل موظف نشط، ومكتوبة لأطرافها.** القراءة عامة لأن `subscribeToAudits`
   مستمع على المجموعة كاملة (3.3). أما الكتابة فمحصورة في `belongsToAudit`: إدارة الجودة، ورئيس
   فريق المراجعة وأعضاؤه، والجهة المُراجَع عليها المسمّاة في `auditeeId`.

   **وما بداخل ذلك ليس مفروضاً في القاعدة.** أيّ طرف من هؤلاء يستطيع — بنداء مباشر خارج الواجهة —
   أن يكتب أي حقل في المستند: أن يعتمد أجوبةً وهو ليس مدير الجودة، أو يقفز بالمرحلة، أو يغلق ملاحظةً
   على نفسه. تسلسل سير العمل كله (من يجيب، من يعتمد، من يحرّك المرحلة، من يغلق ملاحظة) مفروض في
   `src/lib/audit-workflow.ts` وتستدعيه الصفحات، لأنه يعتمد على مصفوفات وبوابات داخل المستند لا
   تستطيع القاعدة تقييمها بثقة. القاعدة تضمن **من له أن يمسّ المستند**، لا **ماذا يفعل به**.

   ولهذا فإن `auditeeId` ليس حقلاً وصفياً: هو ما يفتح الكتابة للجهة المُراجَع عليها. المراجعة بلا
   `auditeeId` مراجعةٌ لا تستطيع إدارتها الرد على ملاحظاتها، وتُظهر صفحة المراجعة تنبيهاً بذلك.
   (الخطة السنوية لم تعد ضمن هذه الفئة — انظر 3.1.)
2. **التفرد السنوي للخطة غير مضمون** — القسم 3.2، بما فيه الحل الجذري.
3. **`DEFAULT_PERMISSIONS` ما عاد يصف الواقع، والصفحتان لا تفحصان الصلاحية.** بعد توسعة القسم 2.2
   لم يعد لهذا أثر أمني — القواعد هي البوابة الفعلية وهي متسقة — لكنه تناقض يجب أن يُحسم في
   الشيفرة: `canManageUsers` ما زال `false` لـ `quality_manager` في `src/types/index.ts` بينما هو
   يُنشئ المستخدمين فعلاً، و`src/app/users/page.tsx` يقرأ `canManageUsers` ولا يستعمله (يعتمد على
   `hasAccess`)، و`src/app/departments/page.tsx` يقرأ `canManageDepartments` ولا يستعمله فيعرض
   أزرار الإدارات والأقسام لكل موظف نشط ثم ترفض القواعد كتابته. الإصلاح: إمّا استعمال الأعلام أو
   حذفها، لا تركها تصف نموذجاً غير المنفَّذ.
4. **النافذة الأولية سلاح ذو حدّين** — القسم 5.0. ما دامت مفتوحة، أول من يفتح `/login` يملك حساب
   مدير النظام. وهي تُغلق نفسها، لكن **إغلاقها ليس تلقائياً بمرور الوقت**: يغلقها إتمام التهيئة،
   أي فعل من المشغّل. لا يوجد تاريخ انتهاء ينقذك إن نسيتها مفتوحة.
5. **`deleteUser` لا يحذف مستند الربط ولا حساب Auth** (`src/lib/firestore.ts`): يحذف مستند
   المستخدم ومستند كلمة المرور فقط. القواعد تعوّض ذلك بشرط وجود مستند المستخدم في `isActive()`،
   فالوصول ينقطع فوراً، لكن يبقى مستند ربط يتيم وحساب Auth حي. احذفهما يدوياً، وراجع
   `check-auth-migration.mjs` الذي يبلّغ عن مستندات الربط اليتيمة.
6. **لا تحقق من ملكية البريد.** التسجيل الذاتي مفعّل، ولا يوجد شرط `email_verified` في أي قاعدة.
   هذا مقبول فقط لأن الحساب بلا مستند ربط لا يساوي شيئاً — إن أُعيد يوماً أي مسار يمنح صلاحية
   استناداً إلى البريد، تعود الثغرة كما كانت.
7. **لا يوجد Admin SDK ولا Cloud Functions** (خطة Spark). لا توجد Custom Claims، ولا تعطيل
   إجباري لحساب Auth من التطبيق، ولا تحقق من جهة الخادم. كل ما نفرضه مفروض في `firestore.rules`.
8. **ضجيج متوقّع في الـ Console**: `initializeSystemAdmin` يستدعي `hasPassword` عند كل تسجيل دخول،
   وقراءة `passwords` مرفوضة الآن **للجميع بمن فيهم مدير النظام**، فيظهر `permission-denied` واحد
   في سجل المتصفح عند كل دخول. الدالة تعيد `'unknown'` ومستدعوها يعاملونها على أنها "لا تتصرف"،
   فلا أثر وظيفي. إزالة هذا الاستدعاء تنظيفٌ مستحق في `src/lib/firestore.ts`.
9. **شيفرة ميتة حول `passwords`**: `setPassword` و `resetPassword` و `verifyPassword` و
   `getPassword` و `hasPassword` في `src/lib/firestore.ts` لم يعد يستدعيها شيء على مسار حي (عدا
   استدعاء `hasPassword` في `initializeSystemAdmin` المذكور في البند 8)، وكذلك `verifyCredentials`
   و `getUserByEmail` لم يعد لهما أي مستدعٍ خارج الملف نفسه. حذفها تنظيف مستحق.
10. **`scripts/README.md` ما زال يقول إن الحسابات تُنشأ تلقائياً عند أول تسجيل دخول بكلمة المرور
    القديمة.** هذا لم يعد صحيحاً بعد القسم 4؛ حساب المشغّل يجب أن يكون قد أُنشئ من لوحة الإعداد
    الأولي (5.0) أو من Console (5.1) أو من صفحة المستخدمين (5.2). كما أن قسم
    `--check-legacy-passwords` فيه صار خياراً يفشل دائماً.
11. **بند الخطة المرتبط بمراجعة لا يمكن فكّ ارتباطه من التطبيق.** المسار 5 يقبل كتابة `auditId`
    مرة واحدة على بند فارغ فقط (القسم 3.1). لو حُذفت المراجعة بقي البند يشير إليها، وتصحيح ذلك من
    Console. هذا مقصود: البديل هو السماح بتحرير بنود خطة معتمدة.
12. **سجل النشاط يضمن هوية الفاعل، لا صدق الوصف.** القاعدة تشترط أن يكون `actorUserId` هو هوية
    الكاتب، فلا يُكتب قيد باسم غيره ولا يُعدَّل قيد ولا يُحذف (القسم 2.3). لكن `summaryAr` و
    `summaryEn` و `entity` و `changes` نصوص يكتبها التطبيق ولا تستطيع القاعدة التحقق منها، فموظف
    نشط يستطيع — بنداء مباشر خارج الواجهة — إضافة قيود صحيحة النسبة إليه وكاذبة المحتوى، أو إغراق
    السجل. لا يمكن إغلاق هذا من العميل: التحقق من محتوى القيد يحتاج جهة خادم لا وجود لها في خطة
    Spark. الضمان القائم هو: **لا انتحال، ولا تنقيح لاحق**.
13. **محاولات الدخول الفاشلة ليست في السجل** — القسم 2.3، الحدّ الأول. مصدرها Firebase Console →
    Authentication.
14. **`scripts/backup-firestore.mjs` لم يواكب هذه المراجعة**: ما زال يُدرج `activeSessions`
    المغلقة (فيفشل دائماً بالرمز `1`) ولا يُدرج `activityLog` الحيّة (فتغيب عن النسخة الاحتياطية).
    التفصيل والإصلاح في القسم 3.4.
15. **تمكين الموظفين معلّق كلياً على رسالة Firebase لتعيين كلمة المرور.** لا توجد في النظام قناة
    بريد أخرى: `sendPasswordResetEmail` في `src/lib/auth.ts` و `src/contexts/AuthContext.tsx` و
    `src/app/login/page.tsx` هي كل ما هناك. إن لم تصل الرسالة (مرشّح بريد الشركة، عنوان خاطئ) فلا
    سبيل للموظف إلى الدخول، ولا سبيل للمسؤول إلى تعيين كلمة مرور له من التطبيق — العلاج الوحيد من
    Firebase Console. الحالة `reset_email_failed` (القسم 5.2) تغطي فشل الإرسال المُبلَّغ عنه فقط،
    لا الرسالة التي «أُرسلت» ولم تصل.

---

## 9. إذا لم تستطع الدخول بعد النشر (rollback)

لا تُعِد نشر قاعدة مفتوحة. القاعدة المفتوحة هي بالضبط ما أوقف النظام في مارس، وهي هذه المرة ستكشف
كل شيء لكل أحد. تحقّق بالترتيب:

1. هل مستند `authUsers/{uid}` موجود بالـ UID الصحيح؟ افتح Console → Authentication وقارن الـ UID
   حرفياً.
2. هل `isActive` قيمة منطقية `true` وليست نصاً؟ وهل الحقول ثلاثة بالضبط؟
3. هل `users/{userId}` المشار إليه موجود فعلاً، وهل `role` فيه مطابق تماماً لـ `role` في مستند
   الربط؟
4. هل القواعد المنشورة هي فعلاً هذا الملف؟ (Firestore → Rules، وقارن التاريخ).
5. هل تسجّل الدخول بالبريد نفسه الذي أنشأت به الحساب؟ الحساب — لا البريد — هو ما يحمل الـ UID.
6. **إن لم تظهر لوحة الإعداد الأولي أصلاً**: هل `users/system-admin-root` موجود؟ وهل يحمل `authUid`
   بالفعل (أي أن أحداً استعمل النافذة)؟ القسم 5.0، الالتزامان 5 و 6.

الخطوات 1-3 كلها قابلة للتصحيح من Console بدون أي تغيير في القواعد، وهذا هو مسار الإصلاح الصحيح.

**إعادة فتح النافذة الأولية عمداً** (بعد اختراق، أو بعد تهيئة فاشلة تركت حالة نصفية) تتم من Console
وحدها: احذف مستند `authUsers` المعني وحساب Auth المقابل، ثم امسح حقل `authUid` من
`users/system-admin-root`. ثم أعد التهيئة **فوراً** — أنت الآن في نافذة مفتوحة من جديد.

**التراجع الكامل عن Firebase Auth لم يعد خياراً واقعياً.** مستندات `passwords` لم تُحذف بيد هذا
التغيير، لكن الشيفرة التي كانت تقرؤها حُذفت: لا `migrateLegacyUser` ولا `verifyCredentials` على أي
مسار دخول. العودة إلى النظام القديم تعني إعادة كتابة طبقة الدخول بالكامل والرجوع إلى تخزين كلمات
المرور في Firestore وإلى قواعد لا تستطيع التحقق من هوية أحد — أي إلى نظام لا يمكن تأمينه من
العميل. إن اضطررت إليه، افعله بعد نسخة احتياطية ناجحة، ووثّق سببه هنا.
